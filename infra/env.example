# DeeperSeek Environment Configuration
# Copy this to .env and fill in values

# ── REQUIRED ──────────────────────────────────────────────────────────────────
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# ── SERVER ────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# ── AUTH / ACCOUNTS ───────────────────────────────────────────────────────────
# Auth mode:
#   'open'        (default) — no login required, preserves legacy single-user behavior
#   'multi_user'  — accounts required; login/register UI shown to unauthed visitors
# AUTH_MODE=multi_user
#
# Optional: if set, new account registrations (after the first admin) must
# provide this invite code. Leave unset for open public registration.
# REGISTRATION_KEY=invite-only-2025
#
# Legacy single-key auth — if set, requests must include header X-API-Key: <key>
# Honored in BOTH auth modes for backwards compatibility with scripts.
# ACCESS_KEY=your-secret-key

# ── OPTIONAL OVERRIDES ────────────────────────────────────────────────────────
# DEEPERSEEK_BACKEND_URL=http://localhost:3000
