# Core HTTP + content extraction
httpx>=0.27
trafilatura>=1.9
pdfminer.six>=20240706
beautifulsoup4>=4.12
lxml>=5.1
markdownify>=0.12

# Imaging / vision
Pillow>=10
imagehash>=4.3

# Memory — semantic embeddings (optional; lazy auto-install)
sentence-transformers>=2.7
numpy>=1.26

# Code intelligence / data
pandas>=2.1
openpyxl>=3.1
pyarrow>=15
# duckdb is lazily imported by data_query; install for fastest SQL path
# duckdb>=0.10

# Charts + documents
matplotlib>=3.8
markdown>=3.5
# PDF: either backend works (weasyprint needs system libs; reportlab is pure-Python)
# weasyprint>=60
reportlab>=4.1

# Audio transcription — at least one of these
# faster-whisper>=1.0     # preferred (CTranslate2)
# openai-whisper>=20231117  # fallback

# Browser automation (page_images, web_browse)
# playwright>=1.44
